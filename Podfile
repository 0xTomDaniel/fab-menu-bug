# Uncomment the next line to define a global platform for your project
platform :ios, '8.0'

target 'fab-menu-bug' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for fab-menu-bug

  pod 'Material'

  target 'fab-menu-bugTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'fab-menu-bugUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
